[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # Reinforce IPv4-first DNS (also set in lib/mongodb.ts at runtime)
  NODE_OPTIONS = "--dns-result-order=ipv4first"

[[plugins]]
  package = "@netlify/plugin-nextjs"

